#ifndef OOSDL_thread_not_running_HPP
#define OOSDL_thread_not_running_HPP

#include <exception>
#include <string>

namespace OOSDL {
	class thread_not_running : public std::exception {
	public:
		virtual ~thread_not_running() throw() {};
		
		virtual const char* what() const throw();
	};
};
#endif
