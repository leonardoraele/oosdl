#ifndef OOSDL_time_limit_exceed_HPP
#define OOSDL_time_limit_exceed_HPP

#include <exception>
#include <string>

namespace OOSDL {
	class time_limit_exceed : public std::exception {
	public:
		virtual ~time_limit_exceed() throw() {};
		
		virtual const char* what() const throw();
	};
};
#endif
