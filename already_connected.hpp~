#ifndef OOSDL_already_connected_HPP
#define OOSDL_already_connected_HPP

#include <exception>
#include <string>

namespace OOSDL {
	class already_connected : public std::exception {
	public:
		virtual ~already_connected() throw() {};
		
		virtual const char* what() const throw();
	};
};

#endif
